# PB-055.1 — Hotfix pulsante Avvia Pixel Bot

Corregge il pulsante **Avvia Pixel Bot**, che puntava ancora allo script storico `RUN_AUTO_005_2.ps1` e quindi poteva usare il vecchio repository OneDrive.

Dopo l'aggiornamento il pulsante:

- usa esclusivamente il repository ricevuto dal launcher, con priorità a `C:\Dev\Pixel-Bot`;
- non crea task, commit o modifiche al codice;
- verifica l'import del pacchetto `pixel_bot` dalla cartella `src`;
- verifica la disponibilità del Developer Agent;
- mostra branch e stato del repository;
- non esegue più la vecchia correzione runner automatico storico.

Il pulsante rosso **LANCIA AUTO-EVOLUZIONE** resta separato e invariato.
